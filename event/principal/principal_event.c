/* LIBS */
#include "../../includes.h"

void empty() {}

/* function to call for each city */
void (*city_start[9])() = 
{
    empty, /* city 0 = Las vegas */
    empty,
    empty,
    empty,
    empty,
    empty,
    empty,
    empty,
    empty,
};